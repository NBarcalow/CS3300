<%= form_with(model: portfolio_project, local: true) do |form| %>
  <% if portfolio_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(portfolio_project.errors.count, "error") %> prohibited this portfolio_project from being saved:</h2>

      <ul>
      <% portfolio_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
